[package]
name = "crypto"
version = "0.1.0"
edition = "2021"

[dependencies]
k256 = { version = "0.13", features = ["ecdsa", "sha256", "hmac"] }
rand_core = "0.6"
thiserror = "2.0"

[dev-dependencies]
hex = "0.4"
